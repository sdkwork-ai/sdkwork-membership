# Generated by the SDKWork web server standard (SDKWORK_WEBSERVER_SPEC.md)
# from server.common.toml + server.cloud.toml; do not edit by hand.
user sdkwork;
worker_processes auto;
pid /run/sdkwork/membership/webserver.pid;
error_log /var/log/sdkwork/membership/webserver/error.log warn;
events {
    worker_connections 1024;
}
http {
    sendfile on;
    keepalive_timeout 75;
    client_max_body_size 1100m;
    server_tokens off;
    gzip on;
    upstream gateway {
        least_conn;
        keepalive 32;
        server sdkwork-api-cloud-gateway:8080;
    }
    server {
        listen 80;
        server_name membership-demo.sdkwork.com membership-demo.birdcoder.com membership-demo.dtupay.com membership-demo.noaper.com membership-demo.sdkwork.cn membership-demo.birdcoder.cn membership-demo.dtupay.cn membership-demo.noaper.cn membership-demo.skubc.com membership-demo.skubc.cn membership-demo.zowalk.com membership-demo.zowalk.cn membership-demo.offer86.com membership-demo.offer86.cn membership-demo.86offer.com membership-demo.86offer.cn;
        include snippets/gateway-api-locations.nonproduction.conf;
        location / {
            root /usr/share/sdkwork/membership/web/pc;
            index index.html;
            try_files $uri $uri/ /index.html;
        }
    }
}
